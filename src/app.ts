const message = `Hello World`;

// Example of a rule violation: missing semicolon
const greeting = "Hi there";

// Incorrect type usage
const someFunction = (value: unknown): void => {
  console.log(value);
};
